// Toggle to read the full cruise from public/positions.json with no backend.
// When true: positions are loaded once from the static file, the live
// "current position" marker and route prediction are disabled, and no API
// calls are made.
export const STATIC_MODE = true;
